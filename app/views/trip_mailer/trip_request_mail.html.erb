<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Hi <%= @driver.name %></h1>
    <p style="margin-left: 6px">
      <%= @rider.name %> has requested to join you during your trip from <%= @trip.origin %> to <%= @trip.destination %> at <%= @trip.start_time.strftime("%B %e, %Y at %I:%M %p") %>.<br>
      Please confirm it from <%= link_to 'here', trip_url(@trip.id) %>
    </p>
  </body>
</html>